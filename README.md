# DZbot RubinOT Premium V5
Reconstrução fiel ao layout visual aprovado pelo proprietário.

## Publicar
Extraia sobre `D:\dzbot-site`:
```powershell
cd D:\dzbot-site
git add .
git commit -m "DZbot RubinOT Premium V5"
git pull --rebase origin main
git push origin main
```
Não use `--force`.
